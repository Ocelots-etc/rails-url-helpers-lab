<div>
  <% @students.each do |student| %>
    <div><%= link_to student.to_s, student_path(student) %>
    <div><%= link_to student, "/student/#{student.id}" %>
    <div><%= link_to(student, student_path(student.id)) %>
  <% end %>
</div>